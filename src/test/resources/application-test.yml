# Test Configuration
server:
  port: 0

spring:
  application:
    name: pro-spring-jms-demo-test

logging:
  level:
    root: WARN
    '[com.prospringjms]': INFO

# Minimal JMS configuration for testing
jms:
  lib:
    enabled: false